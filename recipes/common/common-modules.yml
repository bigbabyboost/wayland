modules:
    - type: rpm-ostree
      install:
        - wl-clipboard
        - nodejs
        - nodejs-npm
      remove:
      # launcher
        - rofi-wayland
      # file manager
      #  - thunar
      #  - thunar-archive-plugin 
      #  - thunar-volman

      # notifications
        - dunst
        
    - type: script
      scripts:
        - installsass.sh
