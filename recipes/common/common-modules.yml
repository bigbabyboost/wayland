modules:
    - type: rpm-ostree
      remove:
      # notifications
        - dunst
        - sddm-themes
        - sddm-wayland-generic
        -sddm
        - libva-intel-media-driver
      install:
        - wl-clipboard
        - nodejs
        - nodejs-npm
        - intel-media-driver

        
    - type: script
      scripts:
        - installsass.sh
