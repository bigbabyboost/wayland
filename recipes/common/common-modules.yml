modules:
    - type: rpm-ostree
      install:
        - wl-clipboard
        - nodejs
      remove:
      # launcher
        - rofi-wayland
      # file manager
        - thunar
        - thunar-archive-plugin 
        - thunar-volman

      # notifications
        - dunst
        
    - type: script
      scripts:
        - installsass.sh
